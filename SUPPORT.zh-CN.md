# 支持政策

[English](SUPPORT.md) | 简体中文

ccset 是一个尽力维护、由单一维护者负责的开源项目。它不提供响应时限、修复时限、
发布日期或长期版本支持保证。仅支持发布到 npm 的最新版本。

## 用户与产品范围

核心用户是正在配置编程 Agent 以使用第三方 Anthropic 兼容 API 服务的个人开发者。
ccset 生成和检查配置；它不会启用提供商、管理提供商的账户，也不认证提供商的 API
兼容性。

本项目不承担：

- 诊断提供商的故障、计费、模型可用性或兼容性问题；
- 找回丢失的 Token 或配置；
- 及时修复未验证或尽力支持平台上的故障；
- 接受每一个功能请求或 pull request；
- 保持与未成文或已变更的 Claude Code 行为的兼容性；
- 提供响应、修复或发布时限。

由 ccset 导致的数据丢失或凭据暴露缺陷按最高优先级处理，但这不构成时限保证。

## 平台

- **支持**：macOS 和 Linux。
- **尽力支持**：WSL 与使用 PowerShell 的 Windows Terminal。
- **不支持**：`cmd.exe`、移动端终端，以及超出普通终端使用范围的特殊 WSL 集成行为。

在 WSL 上，POSIX `0600` 保证适用于写入 Linux 文件系统的设置，不适用于挂载的
Windows 文件系统。在原生 Windows 上，ccset 不管理 NTFS ACL；用户需自行核实其
Claude Code 目录的权限。

发布说明会列出具体验证过的环境。该证据不构成对每个 Agent、Node.js、操作系统、终端
或 shell 版本兼容性的承诺。

## 维护状态

正常的无活动时段不代表项目已被放弃。如果活跃维护确实停止，仓库会明确说明这一点，同时
保持开放，让可信的社区维护者可以提出接管。
